actor {

  let num1 : Int = 34;
  let num2 : Int = 56;

  // function to add two numbers
  public func addition() : async Int{
    return num1 + num2;
  };

  // function to add two numbers
  public query func addition1(number1 :Int, number2: Int) : async Int{
    return number1 + number2;
  };

  

  // function to subtract two numbers
};